@model ReservaQuadras.Models.PessoaAtletica

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

<h4>PessoaAtletica</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @Html.Hidden("PessoaID", ViewData["PessoaID"])
            <div class="form-group">
                <label asp-for="AtleticaID" class="control-label"></label>
                <select asp-for="AtleticaID" class ="form-control" asp-items="ViewBag.AtleticaID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
